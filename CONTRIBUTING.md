# Contributing

- Use clear commit messages (feat/fix/docs/chore).
- Keep commits small and focused.
- Front-end uses Vue + fetch with promises only (no axios).
- Run locally: `npm install` then `npm run dev`.
